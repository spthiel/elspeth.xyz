export default class Component {

    constructor(x, y, width, height) {
        /** @protected */
        this.x = x;
        /** @protected */
        this.y = y;
        /** @protected */
        this.width = width;
        /** @protected */
        this.height = height;
    }

    draw() {
        /* Implement custom drawing here */
    }

}
